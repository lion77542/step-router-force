# 贡献指南

这个项目靠**社区数据**活着。路由器是黑盒，每一条真实观测都在扩大我们对它的认识。

## 你可以贡献什么

### 1. 触发率数据（最有价值）

在 [docs/experiments.md](docs/experiments.md) 追加你的实测数据，或在 [issues](../../issues) 里发"数据点"：

```
环境: Claude Code / 中转站 / 直连 curl
指令方案: 双通道 / 单system / 单用户消息 / 无指令
任务类型: 写代码 / 聊天 / 长对话
触发率: N/M
其他: 空响应次数、走形式(advice_type)次数
```

每条数据都会进实验记录，帮助社区判断真实世界的命中率分布。

### 2. 新发现

- 新的强制手段（参数、协议、字段）
- advisor 的更多内部行为（新的 advice_type、函数调用格式变化）
- **空响应**的根因定位（限流？超时？路由器内部错误？）
- StepFun 平台更新导致的任何行为变化

### 3. 代码

- 其他客户端的接入实现（中转站插件、SDK 封装、其他 CLI）
- 触发率可视化 / 日志分析工具
- hook 脚本的跨平台改进（macOS/Linux 路径、PowerShell 兼容）

### 4. 文档

- 翻译（英文 README）
- 其他协议的示例（Anthropic 格式、流式）

## 提交规范

1. Fork + PR，或直接提 issue
2. PR 附带实测数据或复现步骤（这个项目**不接收无数据支撑的改动**）
3. 保持"诚实边界"精神：任何"接近 100% 触发"的说法必须附完整测试环境与样本量

## 讨论区

- 触发率优化讨论 → 打标签 `trigger-rate`
- 中转站接入 → 打标签 `integration`
- 平台行为变化 → 打标签 `platform-behavior`
